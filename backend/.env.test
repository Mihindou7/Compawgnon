# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Base de données dédiée aux tests d'intégration
# Surcharger dans .env.test.local si la connexion diffère (ex: hors Docker)
DATABASE_URL="mysql://compawgnon:secret@database:3306/compawgnon?serverVersion=8.4&charset=utf8mb4"

# Mailer désactivé : aucun email envoyé pendant les tests
MAILER_DSN=null://null

# MongoDB dédié aux tests (isolé de la DB de dev)
MONGODB_URL=mongodb://mongodb:27017
MONGODB_DB=compawgnon_test
